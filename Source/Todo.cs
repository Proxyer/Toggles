// Alt UI for HugsLib users

// out/ref harmony
//static Type[] ParamTypes { get; } = { typeof(IEnumerable<Gizmo>), typeof(float), typeof(Gizmo).MakeByRefType() };

// DrawGizmoGrid postfix gives weird error. Cant patch? Want to get gizmos.

// private variables underscore _foo

// Transpiler. Opscode.Nop, RemoveRange, or jump? Probably Jump, to retain codes for toggling.
// Seems to be: put bool on stack, consume with brfalse, which moves to target if consumed code is false.

//Binary clock. stardate, other clocks, both realtime and ingame.

//    Like prepare carefully

//    Suppress incident

// Pre release:
//Remove old init db from ToggleHandler
//Mod image
//About doc
//Ludeon blog entry
//discord
//github setup and commit
//avatar image
//remove old unused patches
// test with full mod list